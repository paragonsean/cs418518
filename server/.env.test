# ✅ Use a test database
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=courses_test

# Test Environment
NODE_ENV=test
PORT=9000

# Auth
TOKEN_SECRET_KEY=secretkeyappearshere

# Localhost is safer for CORS in tests
ALLOWED_ORIGINS=http://localhost:3000

# These won't be used in test, but required if referenced
FRONTEND_URL=http://localhost:3000
RECAPTCHA_SECRET_KEY=test
EMAIL_USER=test@example.com
EMAIL_PASS=testpass
EMAIL_HOST=smtp.test.com
